# Client-side config for `npm run client` (vue-cli dev server).
# Only VUE_APP_* variables are exposed to the browser bundle.
# Contains no secrets — this file is committed on purpose.

VUE_APP_GRAPHQL_HTTP=http://localhost:4000/graphql
VUE_APP_GRAPHQL_WS=ws://localhost:4000/graphql
